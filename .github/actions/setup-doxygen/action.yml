name: 'Setup Doxygen'
description: 'Install Doxygen with version that supports Fortran cross-references'
inputs:
  version:
    description: 'Doxygen version to install'
    required: false
    default: '1.16.0'
runs:
  using: 'composite'
  steps:
    - name: Install Doxygen
      uses: ssciwr/doxygen-install@ad81ff2dcb7f3050bc694ed5934f26a16da684d5 # v2.0.0
      with:
        version: ${{ inputs.version }}

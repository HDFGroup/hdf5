name: Test HDF5 Subfiling VFD

on:
  workflow_call:
    inputs:
      build_mode:
        description: "Build type (CMAKE_BUILD_TYPE)"
        required: true
        type: string

permissions:
  contents: read

env:
  CTEST_OUTPUT_ON_FAILURE: 1

jobs:
  build_and_test:
    strategy:
      # Let jobs run to completion even if one fails
      fail-fast: false
      matrix:
        os_name: ["Ubuntu"]
#        os_name: ["Ubuntu", "MacOS"]
        mpi_lib: ["OpenMPI"]
#        mpi_lib: ["OpenMPI", "MPICH"]
        include:
          - os_name: "Ubuntu"
            os: ubuntu-latest
            mpi_lib: "OpenMPI"
#          - os_name: "Ubuntu"
#            os: ubuntu-latest
#            mpi_lib: "MPICH"
#          - os_name: "MacOS"
#            os: macos-latest
#            mpi_lib: "OpenMPI"
#          - os_name: "MacOS"
#            os: macos-latest
#            mpi_lib: "MPICH"

    # Sets the job's name from the properties
    name: "Test HDF5 Subfiling VFD (${{ inputs.build_mode }}) on ${{ matrix.os_name }} with ${{ matrix.mpi_lib }}"

    runs-on: ${{ matrix.os }}

    steps:
      # - name: Install Linux Dependencies
      #  run: |
      #    sudo apt-get update
      #    sudo apt-get install 
      #  if: ${{ matrix.os == 'ubuntu-latest' }}

      # For now, just install OpenMPI or MPICH with the package
      # manager. Eventually, we should pick one or 2 release
      # versions of each, then build and cache those installations
      - name: Install OpenMPI
        run: |
          sudo apt-get update
          sudo apt-get install libopenmpi-dev
          echo "CC=mpicc" >> $GITHUB_ENV
        if: ${{ matrix.os == 'ubuntu-latest' && matrix.mpi_lib == 'OpenMPI' }}

      - name: Install MPICH
        run: |
          sudo apt-get update
          sudo apt-get install libmpich-dev
          echo "CC=mpicc" >> $GITHUB_ENV
        if: ${{ matrix.os == 'ubuntu-latest' && matrix.mpi_lib == 'MPICH' }}

      - name: Checkout HDF5
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Configure HDF5 with Subfiling VFD
        shell: bash
        env:
          RUNNER_WORKSPACE: ${{ runner.workspace }}
          INPUT_BUILD_MODE: ${{ inputs.build_mode }}
        run: |
          mkdir $RUNNER_WORKSPACE/build
          cd $RUNNER_WORKSPACE/build
          cmake -DCMAKE_BUILD_TYPE=$INPUT_BUILD_MODE \
            -DCMAKE_INSTALL_PREFIX=$RUNNER_WORKSPACE/hdf5_build \
            -DBUILD_STATIC_LIBS=OFF \
            -DHDF5_TEST_VFD:BOOL=ON \
            -DHDF5_ENABLE_PARALLEL:BOOL=ON \
            -DHDF5_ENABLE_SUBFILING_VFD:BOOL=ON \
            -DMPIEXEC_MAX_NUMPROCS=2 \
            $GITHUB_WORKSPACE
          cat src/libhdf5.settings

      - name: Build HDF5
        shell: bash
        working-directory: ${{ runner.workspace }}/build
        env:
          INPUT_BUILD_MODE: ${{ inputs.build_mode }}
          RUNNER_WORKSPACE: ${{ runner.workspace }}
        run: |
          cmake --build . --parallel 3 --config $INPUT_BUILD_MODE
          echo "LD_LIBRARY_PATH=$RUNNER_WORKSPACE/build/bin" >> $GITHUB_ENV

      - name: Test HDF5 Subfiling VFD
        working-directory: ${{ runner.workspace }}/build
        env:
          INPUT_BUILD_MODE: ${{ inputs.build_mode }}
        run: |
          # For now, just run the tests directly setup for use with the
          # Subfiling VFD. We can expand on this once the library's tests
          # are better separated into categories for VFD testing.
          ctest --build-config $INPUT_BUILD_MODE \
              -R "MPI_TEST_t_subfiling_vfd|MPI_TEST_t_vfd|H5_ph5_subfiling"

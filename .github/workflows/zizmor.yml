name: zizmor

# Static security analysis for GitHub Actions workflows.
# Findings appear as inline annotations on PRs and in the Security tab.
# Runs zizmor directly via pip to avoid a third-party action dependency.

on:
  push:
    branches: [develop]
    paths: ['.github/**']
  pull_request:
    branches: [develop]
    paths: ['.github/**']

permissions: {}

jobs:
  zizmor:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          persist-credentials: false
      - run: pip install --require-hashes zizmor==1.25.2 --hash=sha256:c4246f1344d8dbeffc044d7bb11b131773a7db7eb57d9073c45942dfd3543a1f
      - run: zizmor --format sarif . > zizmor-results.sarif
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: github/codeql-action/upload-sarif@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4.36.2
        if: always()
        with:
          sarif_file: zizmor-results.sarif
          category: zizmor
